extends layout

include mixins/_projectCard

block content
  .inner
    h1 Playground
    
    //- .items
      //- each menu_item in globals.nestedArticles
      //-   //- h2= menu_item.key
      //-   .projects(id= `#${menu_item.key.toLowerCase()}`)
    .projects
      each item in globals.projects.slice(0,6)
        +projectCard(item)
    .front-section
      a(href="./playground") [See More Projects]
    .front-section
      a(href="https://twitter.com/maptheclouds") [Latest Tweets]
    .front-section
      a(href="https://blog.maptheclouds.com") [Latest Blog Posts]
    .front-section
      div
        a(href="./prints.html") [Prints]

        div(id="rb-xzfcxvzx")
        script(type='text/javascript',  src="https://www.redbubble.com/assets/external_portfolio.js").
        script(type='text/javascript').
          console.log('here')
          if (window.innerWidth < 568 ) {
            new RBExternalPortfolio('www.redbubble.com', 'maptheclouds', 1, 2).renderIframe() 
          } else if (window.innerWidth < 768 ) { 
            new RBExternalPortfolio('www.redbubble.com', 'maptheclouds', 1, 3).renderIframe()
          } else { 
            new RBExternalPortfolio('www.redbubble.com', 'maptheclouds', 1, 4).renderIframe()
          }
      


    
